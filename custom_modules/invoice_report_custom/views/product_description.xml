<odoo>
    <record id="view_product_template_form_inherit_description" model="ir.ui.view">
        <field name="name">product.template.form.inherit.description</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- Add the new description field to the form view of product.product -->
            <xpath expr="//page[@name='general_information']/group/group[1]" position="inside">
                <field name="description"/>
            </xpath>
        </field>
    </record>    <record id="view_product_product_form_inherit_description" model="ir.ui.view">
        <field name="name">product.product.form.inherit.description</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="arch" type="xml">
            <!-- Add the new description field to the form view of product.product -->
            <xpath expr="//page[@name='general_information']/group/group[1]" position="inside">
                <field name="description"/>
            </xpath>
        </field>
    </record>
</odoo>
